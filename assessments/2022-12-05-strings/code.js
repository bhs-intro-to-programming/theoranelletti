const upToX = (s) => {
  return s.substring()
}

const pair = (s1, s2) => {
  return (s1 + ' and ') + s2
}

const containsX = (s) => {
  if (s.substring(s.length, 'x')) {
    return true
  }
  else return false
}

const capitalize = (s) => {
  return s.length(s.toUpperCase, 0)
}