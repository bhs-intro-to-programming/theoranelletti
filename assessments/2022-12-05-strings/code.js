const upToX = (s) => {
  return s.substring()
}

const pair = (s1, s2) => {
  return (s1 + ' and ') + s2
}

const containsX = (s) => {
  if (s.substring(s.length, 'x')) {
    return true
  }
  else return false
}

const capitalize = (s) => {
  return 
}
const slug = (s1, s2) => {
  return s1+s2.toLowerCase((s1 + '-') + s2)
}