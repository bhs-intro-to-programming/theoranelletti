// Some constants you will need. No need to do anything to these
const JUPITER_GRAVITY = 24.79;
const EARTH_GRAVITY = 9.8;
const G = 6.6743e-11;

// Write your code here. Remember that you get partial credit for a
// syntactically correct function skeleton, more credit for a skeleton with a
// reasonable argument list, yet more credit for a working function, and full
// credit for a clear and simple working function.

const itemsLeftOver = (numpeople, numitems) => {
  return
};

const areaOfCircles = (r) => {
  return Math.PI(r ** 2);
};

const volumeOfCube = (length) => {
  return length ** 3;
};

const populationGrowth = (current, growth) => {
  return current * growth;
};

const earnedRunAverage = (earnedruns, innings) => {
  return (earnedruns / innings) * 9;
};

const valueOfJewels = (diamonds, emeralds, onediamondingold, oneemeraldingold) => {
  return
};

const payWitOvertime = () => {
  return
};

const firstClassPostage = () => {
  return
};

const wieghtOnJupiter = () => {
  return
};

const gravity = () => {
  return
};